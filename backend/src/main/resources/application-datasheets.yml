# Activated by the 'datasheets' profile: run the datasheet preflight/backfill as a CLI tool and
# exit instead of starting the web server. See DatasheetBackfillRunner for the options.
spring:
  main:
    web-application-type: none
