package com.clele.parts.mail;

/** Provider names that are not a provider bean. */
public final class MailProviders {

    /** Sending disabled: mails are logged instead. The default, so a fresh install just runs. */
    public static final String NONE = "none";

    private MailProviders() {
    }
}
